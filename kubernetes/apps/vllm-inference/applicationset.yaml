# Environment-selected via the cluster generator (see echo-service for details):
# the destination cluster's `environment` label drives the overlay path. The dev
# overlay swaps the GPU vLLM model for a CPU TinyLlama; prod keeps the GPU model.
apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vllm-inference
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
    - clusters:
        selector:
          matchExpressions:
            - key: environment
              operator: Exists
  template:
    metadata:
      name: vllm-inference
      finalizers:
        - resources-finalizer.argocd.argoproj.io
    spec:
      project: platform-apps
      source:
        repoURL: https://github.com/filipurminsky/platform-core.git
        targetRevision: main
        path: kustomize/overlays/{{.metadata.labels.environment}}/vllm-inference
      destination:
        server: "{{.server}}"
        namespace: apps
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
          - ServerSideApply=true
