apiVersion: v1
kind: Service
metadata:
  name: vllm-inference
  labels:
    app: vllm-inference
spec:
  type: ClusterIP
  selector:
    app: vllm-inference
  ports:
    - name: http
      port: 8000
      targetPort: 8000
