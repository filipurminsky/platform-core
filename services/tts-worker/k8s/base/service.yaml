apiVersion: v1
kind: Service
metadata:
  name: tts-worker
  labels:
    app: tts-worker
spec:
  type: ClusterIP
  selector:
    app: tts-worker
  ports:
    - name: http
      port: 80
      targetPort: http
