apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: tts-worker
spec:
  # maxUnavailable: 1 allows eviction even at single-replica scale.
  maxUnavailable: 1
  selector:
    matchLabels:
      app: tts-worker
