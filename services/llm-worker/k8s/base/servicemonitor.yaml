apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: llm-worker
  labels:
    app: llm-worker
    prometheus: kube-prometheus
spec:
  selector:
    matchLabels:
      app: llm-worker
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
