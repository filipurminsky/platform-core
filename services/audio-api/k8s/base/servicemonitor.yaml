apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: audio-api
  labels:
    app: audio-api
    prometheus: kube-prometheus
spec:
  selector:
    matchLabels:
      app: audio-api
  endpoints:
    - port: http
      path: /metrics
      interval: 30s
